export class Film {

    constructor(
        public type: string,
        public name: string,
        public format: string,
        public res: any,
        public language: string,
        public subtitle: boolean
    ) { }
}
